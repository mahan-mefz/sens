برای کنترل دمای یک سیستم با پایتون، باید ابتدا دمای سیستم را بخوانیم و سپس بر اساس این مقدار، تصمیم‌گیری کنیم که آیا نیاز به انجام عملی (مثل روشن کردن فن یا خاموش کردن سیستم) داریم یا خیر. 

در سیستم‌های لینوکس و مک، می‌توان از داده‌های سنسور دما استفاده کرد. برای خواندن دما، می‌توان از ماژول‌های مختلف مانند `psutil` یا دسترسی مستقیم به سنسورهای دما استفاده کرد.

اینجا یک نمونه کد ساده با استفاده از کتابخانه `psutil` برای کنترل دمای سیستم را ارائه می‌دهم:

### نصب کتابخانه‌های مورد نیاز:
قبل از اجرای کد، ابتدا باید کتابخانه `psutil` را نصب کنید. این کتابخانه اطلاعات سخت‌افزاری سیستم مثل دما، استفاده از پردازنده و حافظه را به شما می‌دهد.

```bash
pip install psutil
```

### کد نمونه برای کنترل دمای سیستم:

```python
import psutil
import time

# تابع برای خواندن دمای سیستم
def get_temperature():
    try:
        temps = psutil.sensors_temperatures()
        if not temps:
            print("سنسور دما یافت نشد.")
            return None
        
        # ممکن است بسته به نوع سیستم، نام سنسور متفاوت باشد. معمولاً 'coretemp' یا 'thermal_zone' استفاده می‌شود.
        for name, entries in temps.items():
            for entry in entries:
                if entry.label == '':
                    return entry.current
        return None
    except Exception as e:
        print(f"خطا در خواندن دما: {e}")
        return None

# تابع برای کنترل سیستم بر اساس دما
def control_temperature():
    # تعریف آستانه‌های دما
    MAX_TEMP = 70  # دما به سانتی‌گراد
    MIN_TEMP = 30

    while True:
        current_temp = get_temperature()

        if current_temp:
            print(f"دمای کنونی سیستم: {current_temp}°C")

            # اگر دما بیش از حد بالا رفت، فن روشن شود یا اقدام خاصی انجام شود
            if current_temp >= MAX_TEMP:
                print("دما زیاد است! روشن کردن فن...")
                # اینجا می‌توانید کد روشن کردن فن یا اقدام دیگر را بنویسید

            # اگر دما پایین‌تر از حداقل بود، فن خاموش شود
            elif current_temp <= MIN_TEMP:
                print("دما پایین است! خاموش کردن فن...")
                # اینجا می‌توانید کد خاموش کردن فن یا اقدام دیگر را بنویسید

        else:
            print("عدم دسترسی به داده‌های دما.")
        
        # هر چند ثانیه یکبار دما را بررسی می‌کنیم
        time.sleep(5)

if __name__ == "__main__":
    control_temperature()
```

### توضیحات کد:
1. **خواندن دما**: تابع `get_temperature` از طریق `psutil` دمای سنسورها را می‌خواند. بسته به نوع سیستم، ممکن است سنسورها متفاوت باشند (مثلاً `coretemp` در سیستم‌های لینوکس).
2. **کنترل دما**: تابع `control_temperature` بر اساس دمای خوانده شده، تصمیم‌گیری می‌کند که آیا فن باید روشن شود یا خاموش.
3. **آستانه‌ها**: در این کد، یک آستانه بالا (۷۰ درجه) و یک آستانه پایین (۳۰ درجه) تعیین شده است. می‌توانید این مقادیر را بر اساس نیاز خود تغییر دهید.

### نکات:
- این کد برای سیستم‌های لینوکس و مک که دسترسی به سنسورهای دما دارند کار می‌کند.
- اگر روی ویندوز هستید، باید از نرم‌افزارهایی مانند HWMonitor یا روش‌های دیگری برای خواندن دما استفاده کنید.
- برای کنترل واقعی فن یا دیگر اجزای سخت‌افزاری، نیاز به دسترسی مستقیم به سخت‌افزار (مثلاً با GPIO در رزبری پای) دارید که در این کد پیاده‌سازی نشده است.

این کد به شما امکان می‌دهد که به صورت ساده دمای سیستم را بررسی کرده و اقدامات لازم را انجام دهید.
